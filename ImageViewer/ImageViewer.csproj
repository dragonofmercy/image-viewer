<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>WinExe</OutputType>
        <TargetFramework>net10.0-windows10.0.22621</TargetFramework>
        <TargetPlatformMinVersion>10.0.22621</TargetPlatformMinVersion>
        <ApplicationManifest>app.manifest</ApplicationManifest>
        <RootNamespace>ImageViewer</RootNamespace>
        <DefaultLanguage>en-US</DefaultLanguage>
        <Platforms>x64</Platforms>
        <RuntimeIdentifier>win-x64</RuntimeIdentifier>
        <SelfContained>true</SelfContained>
        <PublishSingleFile>false</PublishSingleFile>
        <UseWinUI>true</UseWinUI>
        <WindowsPackageType>None</WindowsPackageType>
        <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
        <StartupObject>ImageViewer.Startup</StartupObject>
        <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
        <AppendRuntimeIdentifierToOutputPath>false</AppendRuntimeIdentifierToOutputPath>
        <defineconstants>DISABLE_XAML_GENERATED_MAIN</defineconstants>
        <Description>Image Viewer</Description>
		<Company>Dragon Industries</Company>
		<Authors>DragonOfMercy</Authors>
        <ApplicationIcon>ImageViewer.ico</ApplicationIcon>
		<Copyright>Dragon Industries $([System.DateTime]::Now.ToString('yyyy'))</Copyright>
		<FileVersion>1.7.1.0</FileVersion>
		<Version>1.7.1</Version>
        <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
        <EnablePreviewMsixTooling>true</EnablePreviewMsixTooling>
        <LangVersion>12</LangVersion>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
        <GenerateResourceUsePreserializedResources>true</GenerateResourceUsePreserializedResources>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)'=='Debug'">
        <AssemblyName>ImageViewer.Debug</AssemblyName>
        <AssemblyTitle>Image Viewer (debug)</AssemblyTitle>
        <Product>Image Viewer (debug)</Product>
        <TreatWarningsAsErrors>True</TreatWarningsAsErrors>
        <WarningsNotAsErrors>CA1720</WarningsNotAsErrors>
        <Optimize>False</Optimize>
        <DebugType>full</DebugType>
        <DebugSymbols>true</DebugSymbols>
        <DefineConstants>DEBUG;TRACE</DefineConstants>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)'=='Release'">
        <AssemblyName>ImageViewer</AssemblyName>
        <AssemblyTitle>Image Viewer</AssemblyTitle>
        <Product>Image Viewer</Product>
        <TreatWarningsAsErrors>True</TreatWarningsAsErrors>
        <WarningsNotAsErrors>CA1720</WarningsNotAsErrors>
        <Optimize>true</Optimize>
        <!-- Precompile app + dependencies to native code so the startup path is not JITed.
             Only takes effect on `dotnet publish` with the win-x64 RID (Build-Release.ps1). -->
        <PublishReadyToRun>true</PublishReadyToRun>
    </PropertyGroup>

    <ItemGroup>
        <Content Include="ImageViewer.ico">
            <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
            <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
        </Content>
        <Content Update="Assets\**\*.*">
            <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
            <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
        </Content>
    </ItemGroup>

    <ItemGroup Condition="'$(DisableMsixProjectCapabilityAddedByProject)'!='true' and '$(EnablePreviewMsixTooling)'=='true'">
        <ProjectCapability Include="Msix" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="CommunityToolkit.WinUI.Controls.ImageCropper" Version="8.2.251219" />
        <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.28000.1839" />
        <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.8.260529003" />
        <PackageReference Include="SixLabors.ImageSharp" Version="3.1.12" />
        <!-- SVG rasterization. Renders to a Skia surface, so nothing here needs GDI+ /
             System.Drawing.Common (see Wrapper/Image.cs). ICO goes through WIC instead. -->
        <PackageReference Include="Svg.Skia" Version="5.2.0" />
        <PackageReference Include="Velopack" Version="1.2.0" />
        <PackageReference Include="WinUIEx" Version="2.9.1" />
        <Manifest Include="$(ApplicationManifest)" />
    </ItemGroup>

    <ItemGroup>
        <!-- Grant the test assembly access to internal types (Wrapper.Image, Utilities, Culture, ...) -->
        <InternalsVisibleTo Include="ImageViewer.Tests" />
    </ItemGroup>

    <ItemGroup>
        <!-- Localized .mui folders to keep; every other language is pruned from build and publish
             output. Adding a supported language means adding its folder(s) here. -->
        <MuiKeep Include="en-us;fr-FR;fr;de-DE;de;es-ES;es;it-IT;it;pt-BR;pt;zh-Hans;zh-CN;ru-RU;ru" />
    </ItemGroup>

    <Target Name="PostBuild" AfterTargets="PostBuildEvent">
        <ItemGroup>
            <RemovingFiles Include="$(OutDir)**\*.mui" Exclude="@(MuiKeep->'$(OutDir)%(Identity)\*.mui')" />
            <RemovingFolders Include="@(RemovingFiles->'%(RootDir)%(Directory)')" />
            <!-- Release ships only the app's own symbols and docs, not every dependency's. -->
            <ThirdPartyFilesToRemove Condition="'$(Configuration)'=='Release'" Include="$(OutDir)*.pdb;$(OutDir)*.xml" Exclude="$(OutDir)$(AssemblyName).pdb;$(OutDir)$(AssemblyName).xml" />
        </ItemGroup>
        <RemoveDir Directories="@(RemovingFolders)" Condition="'@(RemovingFolders)'!=''" />
        <Delete Files="@(ThirdPartyFilesToRemove)" ContinueOnError="true" />
    </Target>

    <Target Name="PostPublish" AfterTargets="Publish">
        <ItemGroup>
            <PublishMuiToRemove Include="$(PublishDir)**\*.mui" Exclude="@(MuiKeep->'$(PublishDir)%(Identity)\*.mui')" />
            <PublishMuiFolders Include="@(PublishMuiToRemove->'%(RootDir)%(Directory)')" />
            <PublishFilesToRemove Condition="'$(Configuration)'=='Release'" Include="$(PublishDir)*.pdb;$(PublishDir)*.xml" Exclude="$(PublishDir)$(AssemblyName).pdb;$(PublishDir)$(AssemblyName).xml" />
        </ItemGroup>
        <RemoveDir Directories="@(PublishMuiFolders)" Condition="'@(PublishMuiFolders)'!=''" />
        <Delete Files="@(PublishFilesToRemove)" ContinueOnError="true" />
    </Target>

</Project>
